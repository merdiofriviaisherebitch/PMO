# Third-party code cloned for research/study only — NEVER commit.
# These are GPL/AGPL/other-licensed repos studied read-only for pattern extraction.
research/vendor/

# Local environment / secrets — NEVER commit. Only .env.example (placeholders) is tracked.
.env
.env*.local

# Node / Next.js
node_modules/
.next/
.vercel/
/out/
/build/

# Tooling / test artifacts
*.tsbuildinfo
/coverage/
*.pem
# Deno (Supabase Edge Functions) lockfile — imports are version-pinned by
# specifier; the root-level lock is a transient dev artifact in this Bun/Next repo.
deno.lock

# OS
.DS_Store
